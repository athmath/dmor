[project]
name = "dmor"
version = "0.3.0"
dependencies = ["numpy", "pyomo", "highspy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["dmor"]